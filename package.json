{
    "name": "youtube-disliked",
    "version": "2.0.0",
    "description": "Adds a 'Disliked videos' list to the YouTube UI, powered by your account.",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "watch": "node build.mjs --watch",
        "typecheck": "tsc --noEmit"
    },
    "license": "MIT",
    "devDependencies": {
        "@types/chrome": "^0.0.270",
        "esbuild": "^0.23.1",
        "typescript": "^5.5.4"
    }
}
